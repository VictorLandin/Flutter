import 'package:flutter/material.dart';

class DireccionesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is the Direction history Screen'),
    );
  }
}
